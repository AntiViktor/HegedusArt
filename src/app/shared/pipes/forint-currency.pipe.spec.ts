import { ForintCurrencyPipe } from './forint-currency.pipe';

describe('ForintCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ForintCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
